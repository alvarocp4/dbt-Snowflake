version: 2

models:
  - name: stg_cost_center
    description: "View con las Oportunidades creadas conservando el último Prospecto"
    config:
      tags: [sh_staging, yukon]
    columns:
      - name: opportunity_number
        description: "Identificador único de la Oportunidad"
      - name: id_prospect
        description: "Identificador único del Prospecto"
        tests:
          - not_null
          - unique
          
  - name: stg_opportunity_current_state
    description: "View con las Visitas y el ultimo estado"
    config:
      tags: [sh_staging, yukon]
    columns:
      - name: opportunity_number
        description: "Identificador único de la Oportunidad"
        tests:
          - not_null
          - unique
      - name: id_prospect
        description: "Identificador único del Prospecto"
        tests:
          - not_null
          - unique
          
  - name: stg_lead_medio_canal
    description: "View con los Prospectos con el cambio de Centro de Coste"
    config:
      tags: [sh_staging, yukon]
    columns:
      - name: opportunity_number
        description: "Identificador único de la Oportunidad"
      - name: id_prospect
        description: "Identificador único del Prospecto"
        
  - name: stg_campana_crea
    description: "View con los Prospectos con el cambio de Centro de Coste"
    config:
      tags: [sh_staging, yukon]
    columns:
      - name: opportunity_number
        description: "Identificador único de la Oportunidad"
      - name: id_prospect
        description: "Identificador único del Prospecto"
        
  - name: stg_foto_crea_criteria
    description: "View con los Prospectos con el cambio de Centro de Coste"
    config:
      tags: [sh_staging, yukon]
    columns:
      - name: opportunity_number
        description: "Identificador único de la Oportunidad"
      - name: id_prospect
        description: "Identificador único del Prospecto"
        
  - name: stg_prospect_consolidated
    description: "View con los Prospectos con el cambio de Centro de Coste"
    config:
      tags: [sh_staging, yukon]
    columns:
      - name: opportunity_number
        description: "Identificador único de la Oportunidad"
      - name: id_prospect
        description: "Identificador único del Prospecto"


