version: 2

models:
  - name: vw_stg_cost_center
    description: "View con las Oportunidades creadas conservando el último Prospecto"
    config:
      tags: [sh_staging, yukon]
    columns:
      - name: opportunity_number
        description: "Identificador único de la Oportunidad"
      - name: id_prospect
        description: "Identificador único del Prospecto"
        tests:
          - not_null
          - unique
          
  - name: vw_stg_opportunity_current_state
    description: "View con las Visitas y el ultimo estado"
    config:
      tags: [sh_staging, yukon]
    columns:
      - name: opportunity_number
        description: "Identificador único de la Oportunidad"
        tests:
          - not_null
          - unique
      - name: id_prospect
        description: "Identificador único del Prospecto"
        tests:
          - not_null
          - unique
          
  - name: vw_stg_lead_source_channel
    description: "View con los Prospectos con el cambio de Centro de Coste"
    config:
      tags: [sh_staging, yukon]
    columns:
      - name: opportunity_number
        description: "Identificador único de la Oportunidad"
      - name: id_prospect
        description: "Identificador único del Prospecto"
        
  - name: vw_stg_campaign_crea
    description: "View con los Prospectos con el cambio de Centro de Coste"
    config:
      tags: [sh_staging, yukon]
    columns:
      - name: opportunity_number
        description: "Identificador único de la Oportunidad"
      - name: id_prospect
        description: "Identificador único del Prospecto"
        
  - name: vw_stg_foto_crea_criteria
    description: "View con los Prospectos con el cambio de Centro de Coste"
    config:
      tags: [sh_staging, yukon]
    columns:
      - name: opportunity_number
        description: "Identificador único de la Oportunidad"
      - name: id_prospect
        description: "Identificador único del Prospecto"
        
  - name: vw_stg_prospect_consolidated
    description: "View con los Prospectos con el cambio de Centro de Coste"
    config:
      tags: [sh_staging, yukon]
    columns:
      - name: opportunity_number
        description: "Identificador único de la Oportunidad"
      - name: id_prospect
        description: "Identificador único del Prospecto"


