version: 2

models:
  - name: fact_mktv_visitas
    description: "Tabla con los datos de Visitas"
    config:
      tags: [sh_main, yukon]
    columns:
      - name: opportunity_number
        description: "Identificador único de la Oportunidad"
        tests:
          - not_null
      - name: id_prospect
        description: "Identificador de la Visita"
        tests:
          - not_null
  - name: fact_mktv_transt_visitas
    description: "Tabla con los datos de Visitas"
    config:
      tags: [sh_main, yukon]
    columns:
      - name: opportunity_number
        description: "Identificador único de la Oportunidad"
        tests:
          - not_null
      - name: id_prospect
        description: "Identificador de la Visita"
        tests:
          - not_null
